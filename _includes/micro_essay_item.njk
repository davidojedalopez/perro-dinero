<a class="block no-underline hover:bg-accent-100 dark:hover:bg-foreground-dark p-4 rounded" href="{{ essay.url | url }}">
  <div class="flex">    
    <h2 class="font-black w-full text-accent-500 dark:text-accent-dark-500">{{ essay.data.title }} </h2>    
  </div>
  <time class="text-sm text-accent-600 dark:text-accent-dark-600" datetime="{{ essay.data.published_at | htmlDateString }}">{{ essay.data.published_at | readableDate }}</time>
  {% if essay.data.last_updated_at %} 
    <span class="px-2 text-sm text-accent-600 dark:text-accent-dark-600">·</span> <span class="text-sm text-accent-600 dark:text-accent-dark-600">Actualizado el </span><time class="text-sm text-accent-600 dark:text-accent-dark-600" datetime="{{ published_at | htmlDateString }}">{{ essay.data.last_updated_at | readableDate }}</time> 
  {% endif %}
  <p>{{ essay.data.description }}</p>  
</a>