---
layout: layouts/base.njk
permalink: "posts/{{ page.fileSlug }}/"
---

<article class="py-8">
  {% if cover_path %}
    <img class="rounded mx-auto mb-4" src="{{ cover_path | url }}">  
  {% endif %}  
  <h1 class="">{{ title }}</h1>
  <time class="text-sm text-accent" datetime="{{ published_at | htmlDateString }}">{{ published_at | readableDate }}</time>
  
  <div class="mt-8">
    {{ content | safe }}
  </div>
</article>